rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the Next.js API (Admin SDK).
    // Deny all direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
